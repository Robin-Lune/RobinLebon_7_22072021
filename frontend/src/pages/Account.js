import React from 'react';
import Header from '../components/header';

const Account = () => {
    return (
        <div>
            <Header/>
            <h1>Account</h1>
        </div>
    );
};

export default Account;